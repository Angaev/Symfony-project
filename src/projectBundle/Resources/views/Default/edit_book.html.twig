{% extends 'projectBundle:Default:index.html.twig' %}
{% block title %}
    {{ titleText }}
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-6">
            {{ form(form) }}
            <a href="{{ path('book_delete', {'id': id}) }}" type="button" class="btn btn-danger">Удалить</a>
        </div>
        <div class="col-md-6">
            {{ form(form_cover) }}
            <a href="{{ path('book_delete_img', {'id': id})}}" class="btn btn-danger sm">
                Удалить обложку
            </a>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script src="http://bootboxjs.com/bootbox.js"></script>
{% endblock %}