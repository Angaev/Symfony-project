1)[] не правильно коммитить аватарки пользователей и обложки книг в проект, тк они имеют непосредственную связь с базой данных
//Убрал из гита вск картинки, кроме заглушек

2)[] deleteAvatar.js поправить форматирование
3)[] проверить все js файлы, поправить форматирование, поправить переносы
4)[] Название сущностей сделать с заглавной буквы , например comment->Comment
5)[] BookController форматирование public function viewAction
6)[] неиспользуемая переменная $users = $userRepo->findAll();
7)[]
    if ($user != null)
    {
    $isAdmin = ($user->getRole() == 'ROLE_ADMIN') ? true : false;
    }
    else
    {
    $isAdmin = false;
    }
заменить на тернарный оператор
8)[] метод viewAction слишком большой, нужно разбить
9)[] можно ли projectBundle:Default:books.html.twig заменить на books.html.twig?
10)[] метод editAction слишком большой, нужно упростить, разбить на методы
11)[] bookAddImgAction тоже самое
12)[] неиспользуемая переменная $books = $em->getRepository('projectBundle:book')->findAll();
13)[] где-то в коде египетские скобки, где-то обычные, нужно везде сделать обычные
14)[] Пройтись по всем методам контроллеров, методы должны быть маленькими, умешаться в 30, а лучше меньше, например 20
15)[] Метод private function isAnonymousUser($user) можно упростить, приводя $user к bool
16)[] неиспользуемая переменная $book = $bookRepo -> findAll();
17)[] название класса с больщой буквы addHouseForm, проверь все классы
18)[] UserTypeForm если пишешь add друг по другом, то пиши все add так, а не часть в строчку, часть с новой строки
19)[] нужно поправить форматирование в sql запросах
$rawSql = "SELECT book.id, book.name, book.year, book.image, book.description, .Запрос нужно выровнять, ощущение такой, что он съехал
20)[] в шаблонах убери комментарии (файлы twig)