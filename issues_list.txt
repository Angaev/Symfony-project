1)[V] не правильно коммитить аватарки пользователей и обложки книг в проект, тк они имеют непосредственную связь с базой данных
//Убрал из гита вск картинки, кроме заглушек. Поправил git ignore

2)[V] deleteAvatar.js поправить форматирование

3)[V] проверить все js файлы, поправить форматирование, поправить переносы

4)[] Название сущностей сделать с заглавной буквы , например comment->Comment

5)[V] BookController форматирование public function viewAction
//убрал лишний пробел в заголовке функции

6)[V] неиспользуемая переменная $users = $userRepo->findAll();
//удалил из viewAction

7)[X]
    if ($user != null)
    {
    $isAdmin = ($user->getRole() == 'ROLE_ADMIN') ? true : false;
    }
    else
    {
    $isAdmin = false;
    }
заменить на тернарный оператор

8)[] метод viewAction слишком большой, нужно разбить

9)[X] можно ли projectBundle:Default:books.html.twig заменить на books.html.twig?
//Без полного пути Symfony не понимает какой шаблон использовать, но phpstorm сокращет название

10)[] метод editAction слишком большой, нужно упростить, разбить на методы

11)[] bookAddImgAction тоже самое

12)[V] неиспользуемая переменная $books = $em->getRepository('projectBundle:book')->findAll();
//убрал

13)[] где-то в коде египетские скобки, где-то обычные, нужно везде сделать обычные

14)[] Пройтись по всем методам контроллеров, методы должны быть маленькими, умешаться в 30, а лучше меньше, например 20

15)[V] Метод private function isAnonymousUser($user) можно упростить, приводя $user к bool
//убрал функцию,

16)[V] неиспользуемая переменная $book = $bookRepo -> findAll();
//убрал переменную

17)[] название класса с больщой буквы addHouseForm, проверь все классы

18)[V] UserTypeForm если пишешь add друг по другом, то пиши все add так, а не часть в строчку, часть с новой строки
//все add на новую строку перенес

19)[V] нужно поправить форматирование в sql запросах
$rawSql = "SELECT book.id, book.name, book.year, book.image, book.description, .Запрос нужно выровнять, ощущение такой, что он съехал
//перенес начало запроса на новую строку

20)[] в шаблонах убери комментарии (файлы twig)